package datastructures.SuffixArray;

/*
*
* A suffixArray is an array which contains all sorted suffixed of a string. It is the array of sorted indices.
*
* LCP array - Array in which every index tracks how many characters 2 sorted adjacent suffixes have in common.
*
* */
public class SuffixArray {
}
